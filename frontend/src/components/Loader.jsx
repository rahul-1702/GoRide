import React from "react";
import "./Loader.css"; // Move the styles to a separate CSS file

const Loader = () => {
  return (
    <div className="parent-blob">
      <div className="blob"></div>
    </div>
  );
};

export default Loader;
